<%= domain_namespace %>.<%= object_name %> = Ember.Object.extend({
<%= initialization_list %>,

  serialize:function(){
<%= serialization_setup %>
    return {
<%= serialization_list %>
    };
  }
});